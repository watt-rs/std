use std/rt as rt

pub extern fn println(text: dyn): dyn = "console.log(text)"
pub extern fn readln(): dyn = `
  if (rt.which() == "Deno") {
    return prompt("")
  }
  else {
    return "unimplemented";
  }
`
