use std/rt as rt

pub extern fn println(text: dyn): dyn = "console.log(text)"
pub extern fn input(): dyn = `
  if (rt.which() == "Deno") {
    return new TextDecoder().decode(Deno.readAllSync(Deno.stdin)).trim();
  }
  else {
    return "unimplemented";
  }
`
